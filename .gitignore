node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# User uploads and binary assets - stored outside git
ChatBGP/
uploads/
attached_assets/

# Secrets
.env
.env.local
.env.production

# Build artifacts
*.log

# VOA SQLite snapshots (built locally by scripts/build-voa-sqlite.ts,
# deployed to Railway volume — never committed). Files inside data/ are
# ignored by default, but data/goad/ is whitelisted so the West End Goad
# overlay ships with the repo (~10 MB, totally fine for git).
data/*
!data/goad/
.voa-cache/
.claude/*
!.claude/settings.json
!.claude/hooks/
